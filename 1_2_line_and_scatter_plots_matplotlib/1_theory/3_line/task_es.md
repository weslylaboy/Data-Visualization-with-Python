## Teoría

Esta visualización es mucho mejor, ya que ahora podemos identificar el gran grupo denso en nuestro conjunto de datos. Además, podemos observar una posible relación lineal entre las dos puntuaciones. Para explorar esto más a fondo, intentemos una aproximación lineal de los datos.

Como se mencionó anteriormente, `plot` puede ayudarnos a construir un gráfico lineal. Así que vamos a intentar aplicarlo aquí.

## Tarea

Reemplaza la llamada a `scatter` por `plot`.

Ten en cuenta que no necesitas ajustar la transparencia ni cambiar el color para este gráfico.

## Pistas

<div class="hint" title="¿Cómo debería ser la figura?">
   <img src="example.png" alt="Cómo debería ser la figura" style="max-height: 500px">
</div>